import { SignOutForm } from "@/lib/forms";
import { Button } from ".";

export default function SignOutButton() {
  return (
    <SignOutForm>
      <Button title="Sign out" />
    </SignOutForm>
  );
}
